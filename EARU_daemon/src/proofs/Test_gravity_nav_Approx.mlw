module Test_gravity_nav_Approx

  use int.Int



  let approx (a: int) (b: int) (tol: int) : int
    requires { a >= 0 }
    requires { b >= 0 }
    requires { tol >= 0 }
    ensures { result >= 0 }
    = Abs (a - b) <= tol

end