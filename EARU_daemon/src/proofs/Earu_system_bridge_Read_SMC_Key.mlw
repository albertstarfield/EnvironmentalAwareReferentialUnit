module Earu_system_bridge_Read_SMC_Key

  use int.Int

  use string.String


  let read_SMC_Key (filename: string) : int
    requires { true }
    ensures { true (conversion is total on clamped input). }
    = Earu.IO.Read_Sensor_Real (filename)

end