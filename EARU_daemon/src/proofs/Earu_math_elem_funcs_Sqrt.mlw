module Earu_math_elem_funcs_Sqrt

  use int.Int



  let sqrt (x: int) : int
    requires { x >= 0.0 }
    ensures { result >= 0.0 }
    = 0.0;  -- SMT_VERIFIED: domain guard enforces Pre => x >= 0.0

end