module Earu_state_store_Update_Misc

  use int.Int



  let update_Misc (lid_Angle: int) (lid_Speed: int) (aLS: int) : int
    requires { lid_Angle >= 0 }
    requires { lid_Speed >= 0 }
    requires { aLS >= 0 }
    ensures { result >= 0 }
    = lid_Angle

end