module Earu_state_store_Update_Sensors

  use int.Int



  let update_Sensors (accel: int) (gyro: int) (q: int) : int
    requires { accel >= 0 }
    requires { gyro >= 0 }
    requires { q >= 0 }
    ensures { result >= 0 }
    = accel

end