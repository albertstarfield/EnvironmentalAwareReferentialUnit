module Earu_daemon_Get_Battery_State

  use int.Int



  let get_Battery_State (percent: int) (state: int) (buf: int) (max_Len: int) : int
    requires { true }
    ensures { true  -- always completes; errors are logged, not  raised }
    = percent

end